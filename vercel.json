{
  "version": 2,
  "buildCommand": "cd divina-client && npm install && npm run build",
  "outputDirectory": "divina-client/dist",
  "routes": [
    {
      "src": "^/(.*)$",
      "dest": "/index.html"
    }
  ]
}
